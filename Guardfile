# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'coffeescript', :input => 'assets/javascripts', :output => 'public/javascripts'

guard 'compass' do
  watch('^assets/stylesheets/(.*)\.s[ac]ss')
end
